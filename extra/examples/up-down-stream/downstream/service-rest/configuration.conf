PORT=8778

logging.level.com.github.emw7=debug

# DO NOT TOUCH FROM HERE AFTER

# retrieved from pom.xml
VERSION=x
NAME=x

# spring
#SERVER_PORT=${PORT}
SPRING_APPLICATION_NAME=${NAME}

# dockerization and sidecar
SERVICE=${NAME}
COMPOSE_PROJECT_NAME=${NAME}
SIDECAR_PORT=${PORT}
